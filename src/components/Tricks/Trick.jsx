function Trick({id}){

    return(
        <div>
            <h1>{`${id}`}</h1>
        </div>
    )
};
export default Trick;

// <ul className="trick" id={`${trick.id}`} key={`trick-${trick.id}`}>
// <li>Stance: hello</li>
// </ul>
